# Fork decision log

The repo-root `DECISIONS.md` is upstream's log and is append-only upstream, so
writing fork-specific entries into it would put our lines into a file upstream
rewrites on every release: guaranteed merge surface, in the one file whose whole
value is that it reads as a single project's history. Fork decisions go here
instead. Same format: `[YYYY-MM-DD] <decision>: <why>`.

[2026-08-22] The whole fork layer lives in `fork/` and modifies zero upstream files: every upstream file we touch is a file that must be merged by hand forever, so `git diff upstream/main...HEAD` staying inside one new directory is the property worth optimising for. This is why there is no pointer to fork/README.md in the root README.md and no npm scripts in package.json; the entry point is `npx tsx fork/cli.ts`.
[2026-08-22] Fork decisions live in `fork/DECISIONS.md`, not in the root `DECISIONS.md`: see the paragraph above. Appending to upstream's log would also make the drift guard flag the file, which would be correct and would then have to be suppressed.
[2026-08-22] No npm scripts were added for the fork commands, even though it costs discoverability: `package.json` is upstream-owned and rebased constantly, and the layer is already reachable from `npm test` where it matters (the committed-manifest test), so the script entries would buy convenience at the cost of the zero-modification property.
[2026-08-22] The scheduled sync is a cron job on the box, not a GitHub Actions workflow: GitHub does not run `on: schedule` in forked repositories at all, so a committed workflow would never fire, and `.github/workflows/` is upstream-owned. Prior attempts at this task shipped a workflow YAML that could not have run.
[2026-08-22] The guard is bidirectional: it flags undeclared files AND declared adaptations that stopped differing from upstream. A one-directional check goes green in exactly the case the order cares about, because a reverted file disappears from the diff versus upstream.
[2026-08-22] Adaptation checks live in a declarative manifest (`fork/adaptations.json`) rather than in code: the parser can then enforce invariants across all of them at once, in particular that a tracked adaptation must carry at least one `local-*` check, which is the mechanical form of "our fixes must never disappear silently".
[2026-08-22] The gate preflight is per gate, not all-or-nothing, and every gate always emits a result line: an earlier design skipped three gates when one runner was missing and named only one of them in the report, which is the false all-clear the layer exists to prevent.
[2026-08-22] Upstream's gates run in a throwaway detached worktree at upstream's tip (`--upstream-gates`), not in our checkout: running our own frozen tree and calling the result "upstream is green" would be false, and the order explicitly asks for both suites.
[2026-08-22] `git worktree add --detach` and `git worktree remove --force` are on the git allowlist; everything that could move HEAD, stage work or touch a remote branch is refused and a test walks a full run to prove it. Running upstream's suite is impossible without a worktree, and neither call touches HEAD, the index or any branch.
[2026-08-22] Exit codes rank adaptation (2) above gate (3) above plumbing (4), with 1 reserved for the launcher, but the report always prints one line per category plus a "deliberately skipped" section: the ranking must not be able to hide a second problem.
[2026-08-22] A deliberate skip (`--skip-gates`, no `--upstream-gates`) is a note, not a plumbing alarm; missing dev tooling is a plumbing alarm. A documented choice going red every Monday is alarm fatigue, and alarm fatigue is how a real alarm gets ignored.
[2026-08-22] Alerting updates an existing open alarm issue instead of filing a new one each week, found via a stable `fork-sync-alarm` title marker. A weekly duplicate trains everyone to ignore the alarm.
[2026-08-22] Staleness is itself an alarm (`status`, `maxSyncAgeDays`), and the crontab pings an external dead-man's switch on success: a scheduled job that quietly died looks exactly like one with nothing to report, and nothing running on the box can report that the box is off.
[2026-08-22] `docker-compose.override.yml` stays untracked on each host with a committed, secret-free template under `fork/templates/`: it is per host and it is what people reach for during an incident, which is precisely the change that must not become a fork commit. Upstream's own docs/DOCKER.md already documents the override as the sanctioned mechanism.
[2026-08-22] The 508-commit gap was measured on the old working clone at ed9bdc4, not on the fork, which has zero commits of its own. Catching up is therefore a fast-forward with nothing to conflict, and the documented risk moves to the 370 migrations dated 2026-05-11 or later.
[2026-08-29] The sync verifies the upstream remote's URL against `manifest.upstream.repo` before fetching, and treats a mismatch as plumbing (exit 4) rather than an adaptation alarm. A remote's name is not evidence of its identity: a repointed or inherited `upstream` would otherwise be fetched, diffed and gated against, and the run would report a clean sync of the wrong repository.
[2026-08-29] The documented cron logs to `fork/state/sync.log` inside the clone rather than `/var/log/accounted-fork-sync.log`. The shell opens the redirect as the cron user before the command runs, so an unprovisioned /var/log target kills the job at the redirect every week without ever running the sync.
[2026-09-05] Relocated two decisions from the root `DECISIONS.md` into this file: the SIE 8-year migration validation entry (originally dated 2026-08-22) and the supercronic-entrypoint-hardening entry (also dated 2026-08-22). Both were fork-local calls, written into upstream's append-only log the same day this file's own "fork decisions go here instead" rule was adopted. That is exactly the drift this file exists to prevent: they are the two lines that produced the only merge conflicts in the 2026-09-05 catch-up (see below). Dates preserved as originally recorded.
[2026-09-05] SIE 8-year migration validation compares source vouchers separately from synthetic opening-balance entries, defines turnover as debit turnover, and prefers source #UB/#RES for closing balances: this avoids false voucher-count, doubled-turnover, and derived-balance signals while still failing on every öre-level account difference. (Originally dated 2026-08-22; see relocation entry above.) The instrument itself, `lib/import/sie-migration-validation.ts`, is now declared as adaptation `sie-migration-validation` in the manifest: it is a wholly local file, not present upstream, so it needs a local check or a future merge could delete it silently.
[2026-09-05] Classified the absolute supercronic entrypoint as container hardening rather than a bug fix: the current image supplies a working PATH, but invoking the installed binary by its absolute path removes that runtime dependency. (Originally dated 2026-08-22; see relocation entry above.) The patch itself, `docker/cron.Dockerfile`, is the fork's first real tier 2 adaptation (modifies an upstream file) and is now declared as `docker-cron-entrypoint-hardening` in the manifest. It also landed directly on `main` rather than on `fork/patches` as section 4 of `fork/README.md` prescribes for tier 2 patches: that is a second, separate drift from documented procedure, accepted here as a known deviation rather than unwound, because rewriting already-merged history to move it to a dedicated branch is out of proportion to one two-line Dockerfile change. Future tier 2 patches go on `fork/patches`, per the existing rule.
[2026-09-05] Corrected `fork/README.md` section 2's claim that "the fork has no commits of its own": three commits landed on `main` since that was written (the fork maintenance layer itself, the SIE migration validation instrument, and the self-hosting bug contributions), so an upstream catch-up is no longer guaranteed to be a fast-forward. `git merge --ff-only upstream/main` (README section 6, step 3) failed for exactly that reason during the 2026-09-05 catch-up. Resolution used `git merge upstream/main` instead, on a dedicated branch (`fork-sync/upstream-2026-09-05`, pushed to `origin`, merged into `main` via accounted#5), preserving all three fork commits. The only conflicts were the two append-only blocks in the root `DECISIONS.md` described above; both sides' lines were kept. Section 6 now documents this fallback so the next person hitting a failed fast-forward does not have to rediscover it.
[2026-09-05] Corrected the previous entry below (itself corrected here after cross-model review found the correction still wrong): before assuming upstream's payables features were all missing locally, checked the working tree. All four are present, including the batch service (`lib/payments/batch-service.ts`, `batch-eligibility.ts`, `pain001-supplier.ts`, the `app/api/supplier-invoices/payment-batches/**` routes, the `create_supplier_payment_batch` RPC and its migrations, and three test files), plus the previously-verified pain.001 Swedbank payment file (`lib/salary/payment/pain001-generator.ts`, `app/api/salary/runs/[id]/payment/pain001/route.ts`), duplicate-payment guard (`lib/invoices/duplicate-payment-guard.ts`, `-detection.ts`, `-candidates.ts`), and reminder engine (`lib/invoices/reminder-processor.ts`, `lib/invoices/late-payment-interest.ts`, `lib/bookkeeping/reminder-fee-entries.ts`). All four landed via the upstream merge itself (`git log` on `lib/payments/batch-service.ts` traces to upstream PRs #1500, #1507, #1508, #1989, #2282, not to any fork commit). The payables agent's t1142/t1143/t1144 backlog should be closed as already delivered by this catch-up, not re-scoped to any remaining feature: there is none left to build.
[2026-09-05] Fixed a manifest gap found by cross-model review: `docker-cron-entrypoint-hardening` declared only `docker/cron.Dockerfile`, missing the guard test added in the same original commit (`scripts/__tests__/generate-crontabs.test.ts`, the `describe('cron container')` block asserting the ENTRYPOINT literal). An upstream-owned test file with fork-local content and no declaring adaptation is exactly what `reconcileDrift` treats as an undeclared-drift alarm, which outranks every other exit code: the first real weekly sync would have gone red on this path and stayed red. Added the test file to the adaptation's `paths` with its own `local-contains` check, and to `REVIEWED_OUTSIDE_FORK` in `fork/__tests__/committed-manifest.test.ts`. `upstream.status: "planned"` on this adaptation was also checked against the reviewer's claim that it conflicts with "landed directly on main": it does not, `planned` means not yet contributed upstream, which is true of both the Dockerfile and the guard test regardless of which branch they landed on.
[2026-09-05] Addressed a second cross-model review round. Three fixes: (1) `REVIEWED_OUTSIDE_FORK` in `fork/__tests__/committed-manifest.test.ts` was hand-copied into both the allowlist test and its regression test, so the regression test exercised a copy rather than the guard, reverting the real filter to id-keying would have left it green; hoisted the constant and the shared `findUnreviewedPaths` filter to module scope so both tests run the same code. (2) `fork/README.md` section 6 step 6 still told operators to `git rebase upstream/main fork/patches` unconditionally, but the only tier 2 patch declares `"branch": "main"` (the documented section-4 deviation), so the step now checks each tier 2 adaptation's declared `branch` first and skips the rebase for ones carried on `main`. (3) The catch-up merge itself (`c56e1a5ff`, 725 commits, branch `fork-sync/upstream-2026-09-05`) is now merged into `main` via accounted#5 (the operator's 2026-09-05T00:44 decision to keep it separate from this documentation PR, so CI ran on the full lift before either landed); this file and the manifest are updated in this PR to reflect the post-merge state rather than the pre-merge one.
[2026-09-05] Addressed a third cross-model review round, both findings inside section 3 (the migration table README section 6 itself calls "the risky part"). (1) The section 3 table was still the pre-merge snapshot (370 files, 546 total) even though accounted#5 had by then landed all 725 upstream commits; regenerated it with the command already printed underneath it (`ls supabase/migrations | awk '$0 >= "20260511"' | ...`), which turned up a second, real bug in that command: `ls` also lists the `__tests__/` subdirectory, and the string comparison `"__tests__" >= "20260511"` is true (`_` sorts after digits), so the unfiltered count silently included one non-migration directory entry. Both the table and the two-line command now filter to `grep '\.sql$'` first. Corrected counts: 611 migrations from 2026-05-11 (135/67/171/182/56 across May-September), 786 total in `supabase/migrations/`, range now ending at `20260904163000_fiscal_year_reset_next_year_dependency.sql`. (2) `sie-migration-validation`'s checks only covered `lib/import/sie-migration-validation.ts`; its second declared path, `lib/import/__tests__/sie-migration-validation.test.ts`, had no check of its own, so a revert or an upstream file landing at that exact test path would not have gone red, the same gap already fixed once for `docker-cron-entrypoint-hardening`'s guard test two entries above. Added a matching `local-contains` (anchored on `describe('SIE migration validation'`) and `upstream-absent` pair for the test path, plus a manifest test asserting every path declared under a wholesale-new-file adaptation carries at least one check of its own, not just the adaptation as a whole.
[2026-09-05] Addressed a fourth cross-model review round: the section 3 regeneration in the entry above updated the table itself but missed two other places repeating the stale figure verbatim, plus a scoping gap in the guard meant to prevent exactly this drift. (1) `fork/README.md`'s section 3 "Open operations decision" prose ("the same 370-file jump plus interest") and the section 10 handoff table ("The 370-migration schema jump") both still quoted the pre-regeneration count; both now read 611, matching the table two mentions of the count needed to agree with. (2) The manifest test added in the third round (`gives every declared path of a wholesale-new-file adaptation its own check`) scoped itself to `wholesaleNewFileIds`, so it only ever covered `fork-maintenance-layer` and `sie-migration-validation`; `docker-cron-entrypoint-hardening` (the adaptation whose missing per-path check was the original defect this whole guard exists to catch, per the entry two above) was never included, so a future drop of the `scripts/__tests__/generate-crontabs.test.ts` check from the manifest would still go undetected by this specific test. Renamed the list to `multiPathAdaptationIds` and added `docker-cron-entrypoint-hardening` to it; the test (renamed accordingly) now covers all three multi-path adaptations, not just the wholesale-new-file ones.
[2026-09-05] Corrected the entry above: "two other places" undercounted by one. Section 2's own verification table (`fork/README.md`, the "Migration inventory 2026-05-11 to now" row) still said "370 files, see section 3" after the section 3 table it cross-references had been regenerated to 611; a third stale mention, not two, and precisely the kind of drift the previous entry's fix was meant to close out. Fixed the row to read 611. Since a fix round already claimed this sweep complete once and was wrong, a fix in prose alone was not trusted this time: added a test to `fork/__tests__/docs.test.ts` that parses section 2's row and section 3's computed total out of the README and asserts they are numerically equal, so a future regeneration of one without the other fails `npm test` instead of surviving another review round on the strength of a manual grep.
