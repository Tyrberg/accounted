# Fork decision log

The repo-root `DECISIONS.md` is upstream's log and is append-only upstream, so
writing fork-specific entries into it would put our lines into a file upstream
rewrites on every release: guaranteed merge surface, in the one file whose whole
value is that it reads as a single project's history. Fork decisions go here
instead. Same format: `[YYYY-MM-DD] <decision>: <why>`.

[2026-08-22] The whole fork layer lives in `fork/` and modifies zero upstream files: every upstream file we touch is a file that must be merged by hand forever, so `git diff upstream/main...HEAD` staying inside one new directory is the property worth optimising for. This is why there is no pointer to fork/README.md in the root README.md and no npm scripts in package.json; the entry point is `npx tsx fork/cli.ts`.
[2026-08-22] Fork decisions live in `fork/DECISIONS.md`, not in the root `DECISIONS.md`: see the paragraph above. Appending to upstream's log would also make the drift guard flag the file, which would be correct and would then have to be suppressed.
[2026-08-22] No npm scripts were added for the fork commands, even though it costs discoverability: `package.json` is upstream-owned and rebased constantly, and the layer is already reachable from `npm test` where it matters (the committed-manifest test), so the script entries would buy convenience at the cost of the zero-modification property.
[2026-08-22] The scheduled sync is a cron job on the box, not a GitHub Actions workflow: GitHub does not run `on: schedule` in forked repositories at all, so a committed workflow would never fire, and `.github/workflows/` is upstream-owned. Prior attempts at this task shipped a workflow YAML that could not have run.
[2026-08-22] The guard is bidirectional: it flags undeclared files AND declared adaptations that stopped differing from upstream. A one-directional check goes green in exactly the case the order cares about, because a reverted file disappears from the diff versus upstream.
[2026-08-22] Adaptation checks live in a declarative manifest (`fork/adaptations.json`) rather than in code: the parser can then enforce invariants across all of them at once, in particular that a tracked adaptation must carry at least one `local-*` check, which is the mechanical form of "our fixes must never disappear silently".
[2026-08-22] The gate preflight is per gate, not all-or-nothing, and every gate always emits a result line: an earlier design skipped three gates when one runner was missing and named only one of them in the report, which is the false all-clear the layer exists to prevent.
[2026-08-22] Upstream's gates run in a throwaway detached worktree at upstream's tip (`--upstream-gates`), not in our checkout: running our own frozen tree and calling the result "upstream is green" would be false, and the order explicitly asks for both suites.
[2026-08-22] `git worktree add --detach` and `git worktree remove --force` are on the git allowlist; everything that could move HEAD, stage work or touch a remote branch is refused and a test walks a full run to prove it. Running upstream's suite is impossible without a worktree, and neither call touches HEAD, the index or any branch.
[2026-08-22] Exit codes rank adaptation (2) above gate (3) above plumbing (4), with 1 reserved for the launcher, but the report always prints one line per category plus a "deliberately skipped" section: the ranking must not be able to hide a second problem.
[2026-08-22] A deliberate skip (`--skip-gates`, no `--upstream-gates`) is a note, not a plumbing alarm; missing dev tooling is a plumbing alarm. A documented choice going red every Monday is alarm fatigue, and alarm fatigue is how a real alarm gets ignored.
[2026-08-22] Alerting updates an existing open alarm issue instead of filing a new one each week, found via a stable `fork-sync-alarm` title marker. A weekly duplicate trains everyone to ignore the alarm.
[2026-08-22] Staleness is itself an alarm (`status`, `maxSyncAgeDays`), and the crontab pings an external dead-man's switch on success: a scheduled job that quietly died looks exactly like one with nothing to report, and nothing running on the box can report that the box is off.
[2026-08-22] `docker-compose.override.yml` stays untracked on each host with a committed, secret-free template under `fork/templates/`: it is per host and it is what people reach for during an incident, which is precisely the change that must not become a fork commit. Upstream's own docs/DOCKER.md already documents the override as the sanctioned mechanism.
[2026-08-22] The 508-commit gap was measured on the old working clone at ed9bdc4, not on the fork, which has zero commits of its own. Catching up is therefore a fast-forward with nothing to conflict, and the documented risk moves to the 370 migrations dated 2026-05-11 or later.
[2026-08-29] The sync verifies the upstream remote's URL against `manifest.upstream.repo` before fetching, and treats a mismatch as plumbing (exit 4) rather than an adaptation alarm. A remote's name is not evidence of its identity: a repointed or inherited `upstream` would otherwise be fetched, diffed and gated against, and the run would report a clean sync of the wrong repository.
[2026-08-29] The documented cron logs to `fork/state/sync.log` inside the clone rather than `/var/log/accounted-fork-sync.log`. The shell opens the redirect as the cron user before the command runs, so an unprovisioned /var/log target kills the job at the redirect every week without ever running the sync.
[2026-09-05] Relocated two decisions from the root `DECISIONS.md` into this file: the SIE 8-year migration validation entry (originally dated 2026-08-22) and the supercronic-entrypoint-hardening entry (also dated 2026-08-22). Both were fork-local calls, written into upstream's append-only log the same day this file's own "fork decisions go here instead" rule was adopted. That is exactly the drift this file exists to prevent: they are the two lines that produced the only merge conflicts in the 2026-09-05 catch-up (see below). Dates preserved as originally recorded.
[2026-09-05] SIE 8-year migration validation compares source vouchers separately from synthetic opening-balance entries, defines turnover as debit turnover, and prefers source #UB/#RES for closing balances: this avoids false voucher-count, doubled-turnover, and derived-balance signals while still failing on every öre-level account difference. (Originally dated 2026-08-22; see relocation entry above.) The instrument itself, `lib/import/sie-migration-validation.ts`, is now declared as adaptation `sie-migration-validation` in the manifest: it is a wholly local file, not present upstream, so it needs a local check or a future merge could delete it silently.
[2026-09-05] Classified the absolute supercronic entrypoint as container hardening rather than a bug fix: the current image supplies a working PATH, but invoking the installed binary by its absolute path removes that runtime dependency. (Originally dated 2026-08-22; see relocation entry above.) The patch itself, `docker/cron.Dockerfile`, is the fork's first real tier 2 adaptation (modifies an upstream file) and is now declared as `docker-cron-entrypoint-hardening` in the manifest. It also landed directly on `main` rather than on `fork/patches` as section 4 of `fork/README.md` prescribes for tier 2 patches: that is a second, separate drift from documented procedure, accepted here as a known deviation rather than unwound, because rewriting already-merged history to move it to a dedicated branch is out of proportion to one two-line Dockerfile change. Future tier 2 patches go on `fork/patches`, per the existing rule.
[2026-09-05] Corrected `fork/README.md` section 2's claim that "the fork has no commits of its own": three commits landed on `main` since that was written (the fork maintenance layer itself, the SIE migration validation instrument, and the self-hosting bug contributions), so an upstream catch-up is no longer guaranteed to be a fast-forward. `git merge --ff-only upstream/main` (README section 6, step 3) failed for exactly that reason during the 2026-09-05 catch-up. Resolution used `git merge upstream/main` instead, on a dedicated branch (`fork-sync/upstream-2026-09-05`, pushed to `origin`, merged into `main` via accounted#5), preserving all three fork commits. The only conflicts were the two append-only blocks in the root `DECISIONS.md` described above; both sides' lines were kept. Section 6 now documents this fallback so the next person hitting a failed fast-forward does not have to rediscover it.
[2026-09-05] Corrected the previous entry below (itself corrected here after cross-model review found the correction still wrong): before assuming upstream's payables features were all missing locally, checked the working tree. All four are present, including the batch service (`lib/payments/batch-service.ts`, `batch-eligibility.ts`, `pain001-supplier.ts`, the `app/api/supplier-invoices/payment-batches/**` routes, the `create_supplier_payment_batch` RPC and its migrations, and three test files), plus the previously-verified pain.001 Swedbank payment file (`lib/salary/payment/pain001-generator.ts`, `app/api/salary/runs/[id]/payment/pain001/route.ts`), duplicate-payment guard (`lib/invoices/duplicate-payment-guard.ts`, `-detection.ts`, `-candidates.ts`), and reminder engine (`lib/invoices/reminder-processor.ts`, `lib/invoices/late-payment-interest.ts`, `lib/bookkeeping/reminder-fee-entries.ts`). All four landed via the upstream merge itself (`git log` on `lib/payments/batch-service.ts` traces to upstream PRs #1500, #1507, #1508, #1989, #2282, not to any fork commit). The payables agent's t1142/t1143/t1144 backlog should be closed as already delivered by this catch-up, not re-scoped to any remaining feature: there is none left to build.
[2026-09-05] Fixed a manifest gap found by cross-model review: `docker-cron-entrypoint-hardening` declared only `docker/cron.Dockerfile`, missing the guard test added in the same original commit (`scripts/__tests__/generate-crontabs.test.ts`, the `describe('cron container')` block asserting the ENTRYPOINT literal). An upstream-owned test file with fork-local content and no declaring adaptation is exactly what `reconcileDrift` treats as an undeclared-drift alarm, which outranks every other exit code: the first real weekly sync would have gone red on this path and stayed red. Added the test file to the adaptation's `paths` with its own `local-contains` check, and to `REVIEWED_OUTSIDE_FORK` in `fork/__tests__/committed-manifest.test.ts`. `upstream.status: "planned"` on this adaptation was also checked against the reviewer's claim that it conflicts with "landed directly on main": it does not, `planned` means not yet contributed upstream, which is true of both the Dockerfile and the guard test regardless of which branch they landed on.
[2026-09-05] Addressed a second cross-model review round. Three fixes: (1) `REVIEWED_OUTSIDE_FORK` in `fork/__tests__/committed-manifest.test.ts` was hand-copied into both the allowlist test and its regression test, so the regression test exercised a copy rather than the guard, reverting the real filter to id-keying would have left it green; hoisted the constant and the shared `findUnreviewedPaths` filter to module scope so both tests run the same code. (2) `fork/README.md` section 6 step 6 still told operators to `git rebase upstream/main fork/patches` unconditionally, but the only tier 2 patch declares `"branch": "main"` (the documented section-4 deviation), so the step now checks each tier 2 adaptation's declared `branch` first and skips the rebase for ones carried on `main`. (3) The catch-up merge itself (`c56e1a5ff`, 725 commits, branch `fork-sync/upstream-2026-09-05`) is now merged into `main` via accounted#5 (the operator's 2026-09-05T00:44 decision to keep it separate from this documentation PR, so CI ran on the full lift before either landed); this file and the manifest are updated in this PR to reflect the post-merge state rather than the pre-merge one.
[2026-09-05] Addressed a third cross-model review round, both findings inside section 3 (the migration table README section 6 itself calls "the risky part"). (1) The section 3 table was still the pre-merge snapshot (370 files, 546 total) even though accounted#5 had by then landed all 725 upstream commits; regenerated it with the command already printed underneath it (`ls supabase/migrations | awk '$0 >= "20260511"' | ...`), which turned up a second, real bug in that command: `ls` also lists the `__tests__/` subdirectory, and the string comparison `"__tests__" >= "20260511"` is true (`_` sorts after digits), so the unfiltered count silently included one non-migration directory entry. Both the table and the two-line command now filter to `grep '\.sql$'` first. Corrected counts: 611 migrations from 2026-05-11 (135/67/171/182/56 across May-September), 786 total in `supabase/migrations/`, range now ending at `20260904163000_fiscal_year_reset_next_year_dependency.sql`. (2) `sie-migration-validation`'s checks only covered `lib/import/sie-migration-validation.ts`; its second declared path, `lib/import/__tests__/sie-migration-validation.test.ts`, had no check of its own, so a revert or an upstream file landing at that exact test path would not have gone red, the same gap already fixed once for `docker-cron-entrypoint-hardening`'s guard test two entries above. Added a matching `local-contains` (anchored on `describe('SIE migration validation'`) and `upstream-absent` pair for the test path, plus a manifest test asserting every path declared under a wholesale-new-file adaptation carries at least one check of its own, not just the adaptation as a whole.
[2026-09-05] Addressed a fourth cross-model review round: the section 3 regeneration in the entry above updated the table itself but missed two other places repeating the stale figure verbatim, plus a scoping gap in the guard meant to prevent exactly this drift. (1) `fork/README.md`'s section 3 "Open operations decision" prose ("the same 370-file jump plus interest") and the section 10 handoff table ("The 370-migration schema jump") both still quoted the pre-regeneration count; both now read 611, matching the table two mentions of the count needed to agree with. (2) The manifest test added in the third round (`gives every declared path of a wholesale-new-file adaptation its own check`) scoped itself to `wholesaleNewFileIds`, so it only ever covered `fork-maintenance-layer` and `sie-migration-validation`; `docker-cron-entrypoint-hardening` (the adaptation whose missing per-path check was the original defect this whole guard exists to catch, per the entry two above) was never included, so a future drop of the `scripts/__tests__/generate-crontabs.test.ts` check from the manifest would still go undetected by this specific test. Renamed the list to `multiPathAdaptationIds` and added `docker-cron-entrypoint-hardening` to it; the test (renamed accordingly) now covers all three multi-path adaptations, not just the wholesale-new-file ones.
[2026-09-05] Corrected the entry above: "two other places" undercounted by one. Section 2's own verification table (`fork/README.md`, the "Migration inventory 2026-05-11 to now" row) still said "370 files, see section 3" after the section 3 table it cross-references had been regenerated to 611; a third stale mention, not two, and precisely the kind of drift the previous entry's fix was meant to close out. Fixed the row to read 611. Since a fix round already claimed this sweep complete once and was wrong, a fix in prose alone was not trusted this time: added a test to `fork/__tests__/docs.test.ts` that parses section 2's row and section 3's computed total out of the README and asserts they are numerically equal, so a future regeneration of one without the other fails `npm test` instead of surviving another review round on the strength of a manual grep.
[2026-09-17] Underlagsjakt is a tier 1 extension (`underlagsjakt-extension` in the manifest), not a core change: Mattias's binding requirement, and core edits turn into merge conflicts on every catch-up. Enabling it unavoidably modifies upstream-owned files through the sanctioned seam (`extensions.config.json`, the committed `lib/extensions/_generated/` registry, and the pinned extension count in `lib/extensions/__tests__/sectors.test.ts`); all additive, all declared. Its UI strings are one new `underlagsjakt` namespace in `messages/sv.json` and `messages/en.json`, inserted before `extensions` rather than appended: extension-local message files were tried and rejected, because `i18n/__tests__/message-keys.test.ts` resolves every `t()` key against `messages/*.json` and the only way to keep local files green would be to hide the calls from that guard.
[2026-09-17] The underlagsjakt hand-off to bertil is the files bertil's contract 1.1 already defines, not a new transport: the export is `python -m underlagsjakt --json` read into the workspace, the answers are a downloaded file for `--mottak-svar`. bertil exposes no HTTP endpoint and the extension dispatcher only accepts browser sessions, so any push/pull automation would be invented on both sides.
[2026-09-17] The underlagsjakt settlement choice (vidarefakturera/mellanhavande) is collected and stored in Accounted but left out of the 1.1 answer file: bertil#180 adds the field under a new contract version and is not merged, and silently extending 1.1 is exactly what that task forbids. The answer is only read under versions the extension lists; anything else is refused.
[2026-09-17] Underlagsjakt reuses core's design primitives (dry-table, SegmentedControl, Badge, EmptyState, ConfirmDialog, Select, formatCurrency) but not InvoiceMatchDialog, SupplierInvoicePicker, TransactionAttachDocumentDialog, QuickReviewDialog, SuggestionReviewList or invoice-candidate-ranking: those operate on Accounted's own transactions, invoices and document rows, while bertil's posts and candidates are a different entity that bertil has already ranked. Importing them would mean faking Accounted rows or re-ranking bertil's evidence.
[2026-09-17] `underlagsjakt-extension`'s five checks on `extensions.config.json` and the four `lib/extensions/_generated/` files were permanently red in CI, not because of upstream drift: `.github/actions/setup-core` (pre-existing, used by every core-build.yml job) resets `extensions.config.json` to `{"extensions":[]}` and regenerates the registry before `npm test` runs, so `fork/cli.ts verify` (via `fork/__tests__/cli.test.ts` and `fork/__tests__/committed-manifest.test.ts`, both running inside that same `npm test`) always saw underlagsjakt missing, regardless of what is actually committed. `fork/cli.ts verify` is deliberately offline and makes zero subprocess calls (see `fork/__tests__/sync.test.ts`'s `expect(calls).toEqual([])`), so it cannot shell out to `git show HEAD:<path>` to sidestep the mutated working tree, and `.github/workflows/**`/`.github/actions/**` are upstream-owned and out of scope for a fork-side fix. Widened each of the five patterns to also match the exact, deterministic all-extensions-empty shape `setup:extensions` produces from that reset (e.g. `"extensions":\[\]`, `new Set\(\[\s*\]\)`): that shape only occurs when every extension is gone at once, so a real accidental drop of just underlagsjakt (which leaves the other ~14 in place) still fails the check. Verified both directions locally: the CI reset now passes, and a synthetic revert of only the underlagsjakt entry still alarms with all 5 checks. Rejected removing the four generated-file checks (would stop catching a stale registry after someone edits `extensions.config.json` but forgets to rerun `setup:extensions`) and rejected skipping these checks in `verify` entirely (would downgrade detection from same-day CI, the documented purpose of `fork/README.md`'s "Layer 1", to the weekly `sync` job on the box, up to `maxSyncAgeDays` later).
[2026-09-17] docker-publish.yml's IMAGE_NAME pointed at erp-mafia/gnubok, upstream's GHCR namespace, which this fork's GitHub App installation cannot push to: every push to main since 2026-09-05 failed both platform builds identically (runs 35246610958, 33984840175, 33955376313) with "failed to push ghcr.io/erp-mafia/gnubok: denied: permission_denied: The requested installation does not exist." Moved IMAGE_NAME to tyrberg/accounted, matching this fork's actual GitHub repo rather than upstream's gnubok codename; every other reference in the workflow resolves through env.IMAGE_NAME, so the one env line was the whole fix. Declared as tier 2 adaptation `docker-publish-ghcr-namespace` in the manifest (modifies an upstream-owned file); like docker-cron-entrypoint-hardening it lands directly on main rather than fork/patches, the same documented section 4 deviation, because a patch-series branch for a one-line env value is disproportionate. upstream.status is not-applicable: upstream's own namespace is correct for upstream and this will never be proposed there. Shipped as a PR, not merged directly, pending operator repointing the box's compose file to the new image after the first successful publish.
[2026-09-17] Consequence of the IMAGE_NAME move, recorded because it is not obvious: `.github/workflows/docker-image-scan.yml` still hardcodes erp-mafia/gnubok on line 44. It cannot block the publish (it runs on workflow_run afterwards with packages: read only), and until now its workflow_run arm never fired at all, because it requires conclusion == 'success' and the fork's publish had never succeeded. Once the publish works, that arm starts firing and scans upstream's image instead of the one we just published, so a green run there looks like coverage the fork does not have. docker-publish.yml's own comment (lines 291-296) calls the fork's Trivy step deliberately non-blocking because "the real blocking gate is docker-image-scan.yml, which re-scans the published image" - that sentence is false for this fork from this merge onward. Left out of this change on the one-deviation-per-change rule; queued as a follow-up rather than fixed here, so the divergence is written down somewhere instead of living only in a review comment.
[2026-09-17] The first deploy of our own image showed the extension was not in it. The Dockerfile copies docker/extensions.<preset>.json over the repo-root extensions.config.json before running setup:extensions, so underlagsjakt-extension's registration - which the fork tests check, and which was green - never reaches the built registry. Everything looked correct in source and in CI while the deployed app had no Underlagsjakt entry at all. Added underlagsjakt to docker/extensions.self-hosted.json and declared it as tier 2 adaptation docker-extensions-preset-underlagsjakt with a local-contains check, so the same silent gap fails a test instead of being found by hand after a deploy. The hosted preset (docker/extensions.hosted.json) is deliberately left alone: we only deploy self-hosted.
[2026-09-20] Superseded the 2026-09-17 entry that called the bertil hand-off "files, not a transport": bertil#183 is merged and its client now posts the export to POST /api/extensions/ext/underlagsjakt/export and reads answers from GET /api/extensions/ext/underlagsjakt/svar, so the push/pull is no longer hypothetical on either side. The dispatcher still only accepts browser sessions, which is why those two calls could only ever get a 401. The extension now carries its own machine path (extensions/general/underlagsjakt/lib/leverans.ts): a shared token in UNDERLAGSJAKT_LEVERANS_TOKEN plus the one company it may write to in UNDERLAGSJAKT_LEVERANS_ORGNR, both unset by default so the two endpoints answer 503 until a box turns them on. The file upload moved to POST /export/fil so bertil's URL could stay exactly what it already posts to.
[2026-09-20] Rejected reusing an Accounted API key (gnubok_sk_, lib/auth/api-keys.ts) for that machine path even though it already does company binding, revocation and rate limiting: a key cannot be narrowed to one extension. Scopes live in the core catalogue (lib/auth/scope-catalog.ts) and a key minted with none falls back to DEFAULT_SCOPES, i.e. read access to transactions, invoices, customers, suppliers and every report through MCP and /api/v1; handing bertil the ledger to deliver a JSON file is the opposite of what the task asked for. Narrowing it would mean adding an underlagsjakt scope, plus a machine branch in app/api/extensions/ext/[...path]/route.ts, to two upstream-owned files this fork has to merge forever (README section 4). Also rejected the core cron secret (lib/auth/cron.ts): it is global, so bertil's token would open every scheduled route. The cost of the choice is a token rotated by editing the box's env rather than from the settings UI, which is the same way every other secret on that box is rotated.
[2026-09-20] GET /svar marks the answers it returns as delivered in the same request rather than waiting for bertil to acknowledge them. bertil#183's client does not acknowledge, so the alternative was answers that stay "waiting to be sent" in the workspace forever even after bertil learned them. At-most-once is safe specifically here: an answer bertil failed to ingest leaves the payment unresolved on bertil's side, so its next export asks again, and reconcileWithExport puts the question back in front of the user.
[2026-09-20] The workspace asks whether the delivery lands in THIS company, not whether the box has a token. The first cut read the two env vars and told every company's workspace "bertil levererar exporten hit automatiskt"; on a box with more than one company that is a promise to a user whose extension_data will never receive anything, and the failure is silent on both sides (the export lands in the target company, the waiting user sees no error). leveransTargetsCompany() now runs the same company resolution the delivery itself runs and compares the result to the company being viewed, so a no covers every case that stops a delivery: not configured, org number matching no active company, matching several, or naming another company. Anything but a match says "läs in filen", which is always true.
[2026-09-20] The schedule for the delivery lives in bertil's crontab, not in Accounted's supercronic crontab (docker/crontab.self-hosted, generated from vercel.json). The transport bertil#183 implements is a push: the export client reads bertil's own data and posts it. Accounted cannot pull it, since bertil serves nothing, so an Accounted-side cron entry would have nothing to call. Recorded because the generated crontab is otherwise the obvious place to look for "what runs by itself" in this repository, and this one deliberately is not there. fork/README.md section 11 is the whole switch-on order for both boxes; nothing in this repository can perform steps 1, 3 or 4.
[2026-09-20] The switch-on is verified by a command on the box (extensions/general/underlagsjakt/leverans-status.ts), not by a line in a README. Three of the four switch-on steps happen on machines no pull request can reach (bertil's .env, bertil's crontab, and the first real run), so this repository cannot perform requirements 5 and 6; what it can do is make their absence loud instead of leaving it to a "not live yet" table nobody rereads. The command resolves the company through the same openLeveransContext() a real delivery runs, reads what the two directions actually wrote (imported_via on the stored export, and the leverans journal GET /svar now writes on every call, including polls carrying nothing), and exits 2 until both have carried real data or when more than two days pass with no contact. That last case is the point: a delivery whose cron was never installed and one whose cron died look identical from here, and both are now an alarm rather than silence. Rejected the alternative of proving it once by hand and ticking the table, which is exactly the built-but-never-initiated failure the Definition of Done names.
[2026-09-20] Fixed the disqualifier from the cross-model review of that switch-on check: leverans-status.ts read only process.env, but the documented invocation runs on the host, in the clone, right after the operator put UNDERLAGSJAKT_LEVERANS_TOKEN and UNDERLAGSJAKT_LEVERANS_ORGNR in the deployment's .env, which docker-compose.yml hands to the container through env_file and which never reaches the operator's shell. A correctly configured box therefore reported exit 4 "not switched on" (and, if the operator exported just those two by hand, exit 4 again from createServiceClientNoCookies() with no Supabase credentials), so exit 0 was unreachable, the DoD-#9 gate could never be satisfied, and a dead delivery and a healthy one printed the same thing. The script now loads the deployment .env explicitly the way every other env-dependent tsx script in this repository does (loadDeploymentEnv(), dotenv with an explicit path), searching the working directory then the clone root, and names the file it read in the configuration line so "not switched on" can be told from "looked in the wrong place". override: true, because the question the check answers is what the app runs with, and the app runs with the file; a variable left exported from an earlier attempt must not be able to make the report disagree with the deployment. Rejected the alternative of changing the documented command to carry the env (npx dotenv -e .env -- tsx ...): it adds a package to the invocation and leaves the same trap for anyone who runs the script the obvious way. The env sourcing is now covered by tests (a box configured only through .env reports configured and exits 2, the file wins over a stale shell variable, and a box with no .env says which directory was searched); the previous round tested only describeLeveransStatus with hand-built evidence, which is why this was invisible to the suite.
[2026-09-20] The standing check got a schedule of its own (`/etc/cron.d/underlagsjakt-status`, fork/README.md section 11 step 5) instead of staying a command section 11 called "the standing check afterwards". Nothing ran leverans-status.ts a second time: section 11's cron installs the delivery client on bertil, so the two-day quiet alarm only fired if a human remembered to type the command, which is the exact "the delivery died and nobody noticed" failure the check exists to catch, moved one step further out. Reused section 5's pattern on the same box: cron.d entry, log inside the clone (`fork/state/leverans-status.log`, gitignored and writable by the clone owner, so the shell's redirect cannot kill the job before it runs), and a `curl` chained with `&&` so the external heartbeat is pinged only on exit 0. Its own check UUID, not the sync's: a shared one would let either routine's ping cover for the other's silence. Daily at 07:13, after bertil's 06:17 delivery, because MAX_QUIET_DAYS is 2 and a weekly watcher would find outages up to a week late. Installed in a new step 5, after step 4's first exit 0, since the check answers 2 by design until both directions have carried real data and an early schedule would alarm every morning about a switch-on still in progress. Rejected adding a `gh`-issue path like FORK_SYNC_ALERT_REPO: that is code in the script plus dedupe state, and a check that alarms daily until fixed would file an issue a day; the heartbeat's absence is the alarm and the log holds the report naming which direction stopped. Rejected an Accounted-side supercronic entry (docker/crontab.self-hosted): that file is generated from the `crons` array in vercel.json and every line is a curl to an API route, so it would mean a new core route plus an edit to an upstream-owned file (README section 4's permanent merge surface), and it would run inside the very deployment it is meant to report on, which cannot say "the box is off".
[2026-09-20] Freshness in leverans-status.ts is measured per direction, not as the newest moment across both. The first cut took the max of the stored export's imported_at and the leverans journal's senast_hamtad_at, so a stopped export was held green by bertil's daily GET /svar call (and a broken collection by the exports still arriving): exit 0, and on step 5's schedule a heartbeat pinging healthy every morning over a direction that had been down for weeks, which is worse than no check at all since it actively certifies a dead chain. The two halves stop independently, so each is dated on its own and either one past MAX_QUIET_DAYS takes the exit code off 0; the freshness line names only the half that went quiet, so the log left behind by a missing ping says what stopped. Rejected splitting freshness into the two existing per-direction checks: those answer "has this direction ever carried real data", a different question with a different fix, and folding recency into them would have made one line mean two things.
[2026-09-20] The machine path is now also tested through the real dispatcher (extensions/general/underlagsjakt/__tests__/leverans-dispatch.test.ts), not only by calling the route handlers. The bug this work fixes was never in a handler: it was that app/api/extensions/ext/[...path]/route.ts calls requireAuth() before any handler runs, so bertil's POST could only answer 401. The gap the new file closes is narrower than the first cut of this entry claimed, and the claim mattered because the next agent reads this log as fact: leverans.test.ts does catch a route losing its skipAuth flag (its "are the only two that bypass the session dispatcher" assertion reads the route table), and the dispatcher's own suite has one skipAuth case of its own. What neither covers is the dispatcher ceasing to honour skipAuth: the table assertion passes unchanged, and the dispatcher's case registers a synthetic extension with a session in place, asserting only that the branch returns ahead of the paywall gate. Nothing composed the two, i.e. this extension's delivery, at the URL bertil posts to, carrying nothing but its token. The new file does, driving the dispatcher's exported GET/POST/DELETE with createClient mocked to a session-less client, so any route that falls back to the session gate turns into a 401 the test fails on, and the same session-less caller is what makes the negative cases mean something: the delivery token opens POST /export and GET /svar and nothing else, with all six non-delivery routes answering requireAuth()'s own `{ error: 'Unauthorized' }` envelope rather than the delivery's `{ error: { code } }`, which is what distinguishes "turned away for having no session" from "turned away for its token". The six are asserted to be all of them (a completeness check diffs the list against apiRoutes), so a route added later without skipAuth cannot quietly skip this gate. Verified by mutation rather than assumed, with both numbers measured: making the dispatcher ignore skipAuth fails 4 tests, all 4 in the new file and none pre-existing, which is the regression that was invisible before; removing skipAuth from POST /export fails 6, of which 1 is leverans.test.ts's table assertion. The Supabase stand-in moved to __tests__/supabase-slice.ts rather than being copied into the second file: two hand-written fakes of the same three tables drift, and the copy had already lost the .in() filter recording, which is exactly what proves the export's company comes from the server's configuration and not from the caller's body. Rejected putting the test under app/api/extensions/ext/[...path]/__tests__/ next to the dispatcher's own tests, where it would read more naturally and where the core-build workflow's "no core imports from extensions" grep would not object (it excludes app/api/extensions/): that path is upstream-owned, so a fork-only test file there is a permanent merge surface for the sake of adjacency, while extensions/general/underlagsjakt/ is already declared as a fork path in adaptations.json and needs no new entry.
[2026-09-20] The machine path now says WHICH variable is wrong instead of answering "not switched on" for every shape of bad configuration. readLeveransConfig returned `LeveransConfig | null`, so an unset token, a token short enough to have been typed by hand, an unset org number and an org number that is a company name all collapsed into the same 503 ("Sätt UNDERLAGSJAKT_LEVERANS_TOKEN och UNDERLAGSJAKT_LEVERANS_ORGNR på servern") and the same exit 4 from leverans-status.ts. That message is correct only for the first case, and wrong in the way that costs most: it lands in front of the one person who just wrote both lines into the deployment's .env, and it tells them to write them again. Requirements 5 and 6 are the vakt's, performed on two machines with no test suite between them, so the box's one line about its own configuration is the whole diagnostic; a line that cannot distinguish "unset" from "set wrong" turns a one-character typo into a hunt. inspectLeveransConfig() is now the primitive and reports one sentence per variable that is wrong (naming `openssl rand -base64 24` for a short token, and the accepted org-number shape for a malformed one); readLeveransConfig stays as the thin null-returning wrapper the yes/no callers (leveransTargetsCompany) want, so no call site had to learn a new shape for a question it does not ask. leverans-status.ts's `configured: boolean` became `configProblem: string | null` rather than gaining a second field: two fields that can disagree about the same fact is the next bug. The error CODE is deliberately unchanged (LEVERANS_NOT_CONFIGURED): bertil's client and docs/underlagsjakt-export-schema.md key off it, and the fix is about what a human reads, not about what a machine branches on. Rejected splitting it into per-variable codes for that reason.

[2026-09-20] The org-number complaint from that same work split once more, and the report's headline followed it. `normalizeOrgNumber` refuses a company name and a mistyped check digit through the same `null`, so the one message the previous round added told the operator who typed `556012-5791` to "ange 10 eller 12 siffror, bindestreck valfritt": they count ten digits and one hyphen, conclude the check is broken, and hunt. A mistyped digit is THE one-character typo the message exists to catch, and the repo already owned the primitive that separates it (`hasInvalidOrgNumberCheckDigit`, lib/invariants/org-number.ts, written for exactly this and previously unused here), so the fix is a third branch and not a new rule. `inspectLeveransConfig`'s problems became `{ variable, kind: 'missing' | 'invalid', message }` rather than staying strings, because leverans-status.ts still rendered every configuration problem as "not switched on" under the headline "The automatic delivery is off on this box.": for a box where both lines are present and one digit is wrong that is the same conflation one level up, and it sends the operator to switch on what they already switched on. The kind travels to the report (`configProblem: string | null` became `configProblems: LeveransConfigProblem[]`, so there is still exactly one field and nothing to disagree with itself) and the headline reads "configured wrong" with "Edit that line" instead of "belong in the deployment's .env". Rejected re-deriving the distinction in the report by matching on the message text: that is two places deciding the same thing, and the next message edit breaks it silently. The 503 error code stays `LEVERANS_NOT_CONFIGURED` for the reason recorded above: bertil's client branches on the code, humans read the message.

[2026-09-20] Delivery status keeps English report framing, including all three configuration headlines, while retaining the shared Swedish API error details. This supersedes the earlier blanket "configured wrong" / "Edit that line" advice: a mixed configuration needs an Add action for the missing variable and a Correct action for the invalid variable, not an instruction to edit a nonexistent line. Preserve the per-problem kind and variable rather than collapse them into one invalid flag. The no-file source is a complete sentence, not a filename prefixed with Read. The step-2 runbook now describes all three states.
[2026-09-20] Forsta skarpa leveransen fran bertil gav 401 bade med och utan nyckel, och ingenting nadde extensionen. Orsaken var inte rutten - den deklarerar skipAuth korrekt och authenticateLeverans ar sund - utan att MFA-grinden i lib/supabase/middleware.ts kor FORE dispatchern och bara slapper igenom sokvagar i BEARER_AUTH_PREFIXES, som listade /api/v1/ och mcp-serverns rutt. Lagt till /api/extensions/ext/underlagsjakt/ och deklarerat det som tier 2 (leverans-mfa-gate-prefix). Lardom: en maskinvag ar inte verifierad forran nagon kort ett riktigt HTTP-anrop mot den publicerade sajten. Bygget testade handlern, inte vagen.
